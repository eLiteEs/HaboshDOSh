# haboshdosh
16-bit OS made using assembly and own bootloader and kernel.

## Build
### Disk Image (.img)
For creating a disk image, just run `make`

### DVD Image (.iso)
For creating a .iso, run `make iso`